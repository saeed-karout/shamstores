# Cloudflare static assets deployment for the Vite frontend.
# This makes `npx wrangler deploy` work with the built `dist` folder.

name = "digital-menu-saas-frontend"
compatibility_date = "2026-05-17"

[assets]
directory = "./dist"
